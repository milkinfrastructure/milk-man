[project]
name = "milk-jobs"
version = "0.1.0"
description = "Bounded deterministic Milk reconciliation for Milk Man"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/milk_jobs"]
